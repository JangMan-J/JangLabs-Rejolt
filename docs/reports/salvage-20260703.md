# Salvage report — bolt

**Date:** 2026-07-03 · **Pipeline:** `~/JangLabs/jskills/workflow/WORKFLOW.md` v1.4
**Repo:** `~/JangLabs/bolt` (`JangMan-J/JangLabs-Bolt`, branch `main`)
**Invocation args:** "This repo should be mostly clean docs, check it for conformity before I proceed"

## Verdict: **partial**

Seed a D-ledger from `CORE-SPEC.md`, grill the six known wire-format residuals
closed via `/distill` (spec-slot reconstruction only), then enter the pipeline
prospectively at `/blueprint` for the reseed implementation. No retroactive
`/vet`/`/ship` — there is no code to walk back. Details in §5.

## 1. Future-work driver (assumption — user not queried mid-run)

**Assumed next work:** close the six wire-format gaps in `CORE-SPEC.md` named
by the 2026-06-20 readiness handoff, then implement the routed-memory reseed
from the finished spec. Evidence: the handoff's "Suggested Next Steps"
(`~/JangLabs/.handoffs/.bolt/bolt-core-spec-readiness-handoff-20260620.md`,
lines 51–73), README's framing of the spec as the deliverable, and the
invocation's "before I proceed".

**Sensitivity:** if the owner does *not* intend to implement (bolt stays an
archival spec artifact), this verdict degrades to **no-op** — the repo is
already in good shape as a frozen reference and retrofit buys nothing.

## 2. Archaeology — de-facto artifact inventory

Repo contents: `README.md`, `CORE-SPEC.md` (741 lines), `CONTEXT.md`,
`CLAUDE.md`/`AGENTS.md`, `docs/agents/{domain,issue-tracker,triage-labels}.md`,
empty `docs/adr/`. No code, no tests, no CI, no `docs/frozen/` (precondition
for salvage passes — not yet adopted). No open GitHub issues. Synapse source
paths referenced by the spec all resolve: 19 ADRs, `_PENDING-FROM-GSD.md`,
both promoted specs, `lib/memory_surface.py`.

## 3. Gap map

| Slot | Grade | Best evidence |
|---|---|---|
| **Spec** | **partial** (strong content, wrong shape) | `CORE-SPEC.md` is a genuine de-facto spec — frozen invariants (§2), tunables-vs-invariants partition (§10), non-goals fence (§12), conformance matrix (§14), superseded-rationale appendix. But: status line says "reseed draft", no `## D<n>.` trace anchors, not in `docs/frozen/`, not FROZEN, and six wire formats are implicit (handoff §"Specific Gaps"). |
| **Plan** | **absent** | No plan artifact, no P-items. Commit history is docs-sync-shaped (`git log`: 9 commits, all docs/chore). The handoff names the next patch but nothing is decomposed or traced. |
| **Verification** | **absent here / designed-for-later** | No tests, CI, or verify script in this repo. `CORE-SPEC.md` §14 is a *conformance matrix for the future implementation* — verification design, not a runnable check. Nothing trace-lintable exists yet. |
| **Continuity** | **present** | `.handoffs/.bolt/bolt-core-spec-readiness-handoff-20260620.md` — follows the workspace convention and records the exact readiness gaps and suggested next skills. |
| **Stakes input** | none live | No deploy signals, units, or consumers. The repo itself is a spec artifact; the *downstream implementation* will touch the owner's live memory store. |

## 4. Tier of the planned work (recommend — confirm at `/distill` per §2 UX rule)

**Next change (wire-formats spec patch): T1**, decision-dense.
Blast 1 (one doc, but it is the contract) · Reversibility 0 · Decision density
2 (six open schema decisions) · Stakes 1 · Duration 1 → total 5.

**The reseed implementation that follows: T2.**
Blast 2 (whole subsystem: engine + adapters + bootstrap) · Reversibility 1 ·
Decision density 1 (post-freeze) · Stakes 1 (owner-daily-use memory store) ·
Duration 2 (multi-session / WP fan-out) → total 7.

Since the future work is T1→T2, conformity value is high — this is not a
T0-shaped repo.

## 5. Prescription (command sequence — salvage never executes this)

0. **Housekeeping first (T0, no pipeline, effort: low).**
   - Commit the pending loader refactor (`M CLAUDE.md`, `?? AGENTS.md`) — its
     content already conforms to the home `AGENTS.md` loader rule.
   - Fix `CORE-SPEC.md:8`: "pinned by the parent JangLabs workspace gitlink at
     `26f1691…`" is the one stale-submodule residue the recent doc sweep
     (commits `f83db85`, `ac434f3`, `02961c8`) missed — the workspace root is
     not a git repo, so no gitlink exists. The SHA itself is a valid synapse
     commit; restate it as a plain pin ("distilled from synapse at commit
     `26f1691`"). Note: synapse HEAD is now `81701c2`, **one commit past the
     pin** — re-diff `lib/memory_surface.py` against the pin (or re-pin)
     before treating it as the conflict tiebreaker.
1. **`/distill` — spec-slot reconstruction + residual grill (effort: medium).**
   Draft-in-hand entry with `CORE-SPEC.md` as the draft and this report as
   input; engine `grill-us` (live-default; `grilling` fallback if Codex
   transport is down). Mechanically seed the D-ledger from the seeds in §6
   (that part is low-effort conversion, not re-derivation), then spend the
   grill exclusively on residuals R1–R7. Output:
   `docs/frozen/routed-memory-reseed-decisions-YYYYMMDD.md`, tier recorded in
   the header after owner confirm.
2. **`/blueprint` — implementation plan, T2 ceremony (effort: medium).**
   P-items traced `←` to the frozen Ds; fold §14's conformance matrix into the
   plan's verification design (each matrix row becomes a gated check); red-team
   fan-out per the T2 gate matrix; trace-lint green; freeze-commit.
3. **`/build` → `/vet` → `/ship`** prospectively, per the frozen plan (effort:
   high, but ordinary forward pipeline — not part of the retrofit).

## 6. Reconstruction seeds — inferred D-candidates

Per the provenance rule: any ledger built from these must carry
`RECONSTRUCTED <date> from docs/reports/salvage-20260703.md`, and each entry
cites its evidence below — never presented as contemporaneous owner intent.

| Seed | Decision (inferred) | Evidence |
|---|---|---|
| D-a | Write-time intelligence, read-time index-only lookup | `CORE-SPEC.md` §2.1, §5; `synapse/docs/adr/0004` |
| D-b | Store is source of truth; catalog is a disposable, never-migrated build artifact | §2.2, §4; ADR 0008 |
| D-c | Route on observed behavior, never prompt text; a tag *is* its evidence | §2.3, §3; ADR 0003 |
| D-d | One matcher: recall and collision projection share `_walk_index` | §2.4, §7; ADR 0015 |
| D-e | Precision over recall; surface-gate rule form (≥1 strong OR ≥2 total) | §2.5, §5; ADR 0005 |
| D-f | Fail open everywhere; exactly one fail-closed boundary (full-file frontmatter-bearing memory writes), enumerated deny reasons | §2.7, §2.9, §6; ADR 0011 |
| D-g | Self-curation demotes/flags only, never deletes; three floors (zero-fire, min-evidence, seat dual-gate) | §2.8, §8; ADR 0006, 0007 |
| D-h | Collision verdict reads `live_levers` (routability), not co-fire count; strict `>` floor | §7, Appendix A; ADR 0019 |
| D-i | Perf gate is regression-relative (`baseline + max(25%, 15ms)`), not an absolute cliff | §9; ADR 0018 |
| D-j | Tunables-vs-invariants partition: contract is shape, magnitudes are config | §2 preamble, §10 |
| D-k | Non-goals fence: no GSD, no SQLite/FTS5, no embeddings/LLM on read path, no second matcher, no review ritual | §12; ADR 0002 |
| D-l | Bootstrap contract: idempotent `bootstrap-store`, empty-grammar seed, `byMemoryId` fallback lifecycle, fail-open checks | §13 — **[DESIGNED], weakest provenance; prime grill target** |
| D-m | Stdlib-only engine; all store mutations atomic (write-temp-then-rename) | §0, §2.11 |
| D-n | Engine never writes host permission policy | §0, §12, §13 |
| D-o | Adapter discipline: quiet on success; direct CLIs invert (loud, fail closed) | §2.10, §10; ADR 0014 |

**Residuals for the grill** (each is a named-but-unspecified wire format; source:
handoff lines 40–59 — these are the handoff's own findings, adopted here):

- R1 `normalized operation` — prose only; needs an input schema.
- R2 memory frontmatter — `metadata.triggers` defined, but the tag field/schema
  that dedup and placement rely on is not.
- R3 `_grammar.md` — concrete markdown syntax beyond the empty seed undefined.
- R4 `_memory_catalog.json` — full JSON schema (memories, index values,
  reports, fingerprint, sources) undefined.
- R5 dedup mark — load-bearing for telemetry correlation, but no file/path/
  schema/lifecycle defined.
- R6 `derive_fallback_triggers` — "backtick-quoted tokens" is not deterministic
  enough for legacy import.
- R7 CLI/API surface — command names, outputs, exit codes (handoff's suggested
  "Wire Formats / Public Surfaces" section).

## 7. Predicted outcomes

**What conformity buys here:**

- **G1 mechanizes the handoff's judgment.** The 2026-06-20 review concluded
  "not ready for `/implement`"; a FROZEN-gated ledger makes that a hard
  precondition instead of advice that a future session can skip.
- **Trace anchors for a T2 build.** The implementation plan gets `← D<n>` refs
  and an orphan ledger; §14's matrix rows become per-packet gates. Without
  this, a multi-session build over a 741-line spec drifts exactly the way
  synapse did — which is the stated reason bolt exists.
- **The amendment channel (G4) will be exercised.** Six wire formats are
  implicit and the tiebreaker engine has already drifted one commit past the
  pin; build-time contradictions are near-certain. With the pipeline they land
  as A-entries; without it they land as silent spec drift.

**What it costs:** one grill session plus low-effort ledger conversion. Main
risk: ceremony without payoff if implementation is deferred indefinitely (see
§1 sensitivity — then do nothing; the repo is fine as-is).

## 8. Conformity findings (the invocation's literal ask)

The repo **is** mostly clean docs and conforms well: lab layout matches its
README, agents docs are in place, the handoff follows the workspace
convention, publish flow (own remote, commit+push inside `bolt/`) is correct,
and the empty `docs/adr/` is conforming (lazy creation per
`docs/agents/domain.md`). Three deviations, all small:

1. **`CORE-SPEC.md:8` stale gitlink framing** (§5 step 0) — the one leftover
   from the pre-promotion submodule era; contradicts the repo's own README and
   the last three commits' corrections.
2. **Synapse pin drift** — pin `26f1691` is valid but synapse HEAD (`81701c2`)
   is one commit ahead; the "live engine as tiebreaker" claim needs a re-diff
   or re-pin.
3. **Uncommitted loader refactor** (`M CLAUDE.md`, `?? AGENTS.md`) — content
   conforms to the home-level loader rule; just needs a commit.
